services:
  - type: web
    name: wargame
    runtime: node
    plan: free
    buildCommand: chmod +x build.sh && ./build.sh
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: VITE_SERVER_URL
        value: https://wargame.onrender.com
